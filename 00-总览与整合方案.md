# WorkBuddy × DeepSeek Harness：四仓库分析与"整合成一个功能更全的 DSH 插件"方案

> 分析对象（已克隆至本目录）：
> | 目录 | 上游仓库 | 语言/形态 |
> |---|---|---|
> | `1-dsh-workbuddy-connect` | [corrinehu/dsh-workbuddy-connect](https://github.com/corrinehu/dsh-workbuddy-connect) | TypeScript · **DSH 插件**（Node host 半 + React 浏览器半） |
> | `2-workbuddy2api` | [Sliverkiss/workbuddy2api](https://github.com/Sliverkiss/workbuddy2api) | Go · **上游协议权威实现 + OpenAI 兼容网关（账号池）** |
> | `3-workbuddy2api-gui` | [linbeize/workbuddy2api-gui](https://github.com/linbeize/workbuddy2api-gui) | Go + React(Vite) · **workbuddy2api 的 Web 控制台** |
> | `4-workbuddy-manager` | [ithtelab/workbuddy-manager](https://github.com/ithtelab/workbuddy-manager) | Python(FastAPI) + Next.js · **workbuddy2api 的运营级控制台 + 自带 /v1 网关** |
>
> 全文引用格式为 `相对路径:行号`。

---

## 0. 概念确认（先对齐）

这四个项目**不是同一层的东西**，这是整个分析的关键：

```
                         ┌─────────────────────────────────────────┐
                         │  DeepSeek Harness（DSH）宿主进程           │
                         │  ┌───────────────────────────────────┐  │
   [1] dsh-workbuddy-   │  │ workbuddy 插件                     │  │
       connect          │  │  ├ 读桌面 App 的 auth 文件（只读）    │  │
       （本机 · 单用户）   │  │  ├ 自己刷新 token                   │  │
                         │  │  ├ loopback shim 127.0.0.1:随机端口  │  │
                         │  │  ├ 注册 pi-ai provider 进 DSH LLM 缝 │  │
                         │  │  ├ 注册 settings.section 独立设置板块 │  │
                         │  │  └ 同源 status/probe/usage 路由       │  │
                         │  └───────────────────────────────────┘  │
                         └────────────────┬────────────────────────┘
                                          │ 直接打腾讯上游
                                          ▼
                              copilot.tencent.com / workbuddy.ai

   ─────────────── 另一条完全不同的链路 ───────────────

   下游任意 OpenAI SDK ──► [2] workbuddy2api 网关（Go · 账号池）
                                │  ▲
                                │  └── [3] workbuddy2api-gui    （只读/写 config + docker 控制）
                                │  └── [4] workbuddy-manager    （读 auths/ + 写 config + docker + 自带 /v1 密钥层）
                                ▼
                          腾讯上游（多账号轮询）
```

所以：

- **[1] 是 DSH 的一个插件**，不是网关。它没有账号池、没有对外端口、没有密钥分发、没有访问日志——它的"用户"只有 DSH 自己，部署形态是用户本机。
- **[2] 是反代网关**，[1] 的注释明确承认它是"参照实现"（`1-.../src/upstream.ts:4-6`：*"The wire behavior is ported from Sliverkiss/workbuddy2api (MIT), whose Go implementation is battle-tested against the real endpoint"*），README 致谢也列了它（`1-.../README.md:137`）。
- **[3] 和 [4] 都是 [2] 的控制台**，不是独立能力源。[4] 的 README 自述：*"本项目是为它做可视化的配套项目"*，且用 `- ../workbuddy2api:/opt/workbuddy2api` bind mount 直接操作上游仓库目录（`4-.../docker-compose.yml:97`）。

**因此"把 3 和 4 整合到 1" 不存在"移植代码"这条路**——它们是 Web 控制台 + Python/Go 服务，[1] 是跑在 DSH 宿主里的 Node 插件。真正要做的是**决策**：哪些能力在插件形态下用得上（→ 自己重写为插件的一部分），哪些能力在插件形态下根本无意义（→ 不该进，应该让 [1] 变成 [2] 的客户端）。

下面给出逐项判定。

---

## 1. 问题一：[1] 到底从 [2] 借鉴了什么

结论：**只借鉴了"上游协议线格式"这一层，完全没有借鉴"账号池/网关"那一层。** 逐项证据如下。

### 1.1 已确认逐字借鉴（有代码级对照）

| # | 借鉴内容 | [1] 的位置 | [2] 的位置 | 证据强度 |
|---|---|---|---|---|
| 1 | chat 端点 `/v2/chat/completions` | `src/upstream.ts:574` | `internal/upstream/client.go:964` | 逐字 |
| 2 | 刷新端点 `/v2/plugin/auth/token/refresh` | `src/upstream.ts:595` | `internal/upstream/client.go:892` | 逐字 |
| 3 | CN 模型目录 `/console/enterprises/personal/models` | `src/upstream.ts:636` | `internal/upstream/client.go:1155` | 逐字 |
| 4 | 国际模型目录 `/v3/config` | `src/upstream.ts:636` | `internal/upstream/client.go:1157` | 逐字 |
| 5 | 积分端点 `/v2/billing/meter/get-user-resource` | `src/upstream.ts:702` | `internal/upstream/client.go:809` | 逐字 |
| 6 | **积分请求体**：`ProductCode:"p_tcaca"`、`Status:[0,3]`、`PageSize:100`、`PackageEndTimeRangeEnd = now + 365*101 天` | `src/upstream.ts:705-712` | `internal/server/global_e2e_test.go:257-264` | **逐字，含 365×101 这个怪数** |
| 7 | `X-No-User-Id` / `X-User-Id` 二选一约定 | `src/upstream.ts:349` | `internal/upstream/headers.go:234` | 逐字 |
| 8 | `X-No-Enterprise-Id` / `X-Enterprise-Id` 二选一 | `src/upstream.ts:350-352` | `headers.go:200,243,245` | 逐字 |
| 9 | `X-No-Department-Info` / `X-Domain` 二选一 | `src/upstream.ts:353` | `headers.go:250` | 逐字 |
| 10 | `X-Enterprise-Id` + `X-Tenant-Id` 双写（billing 路径） | `src/upstream.ts:381-382` | `headers.go:415-416` | 逐字 |
| 11 | `X-Product: SaaS` | `src/upstream.ts:354` | `headers.go:348` | 逐字 |
| 12 | **硬积分耗尽关键词表**（9 条英文 + 6 条中文：积分不足/额度不足/余额不足/积分用完/额度用尽/没有积分） | `src/upstream.ts:184-189` | `internal/upstream/client.go:137-141` | **逐字全等** |
| 13 | 错误分类语义：402→硬积分、429→软限流、404→not_found、5xx→server | `src/upstream.ts:290-304` | `client.go` 的 `hardRule`/`softRateRule`/`ErrHardCredit` 分层 | 同构 |
| 14 | 模型行字段语义：`credits`、`tags` 里 `badge:限时免费:#FF0000`、`onlyReasoning`、`supportsReasoning`、`reasoning.supportedEfforts`、`reasoning.defaultEffort`、`supportsImages`、`maxInputTokens`、`maxOutputTokens` | `src/upstream.ts:198-284, 980-1002` | `internal/server/handler_models_full_fields_test.go:12` | **同源 fixture**（含 `reasoning` 对象结构） |
| 15 | 出站 UA 三段式形状 `WorkBuddy/<v> <product>/<v> CLI/<cli>`（chat 路径） | `src/client-identity.ts:122-133` | `internal/upstream/headers.go:14`、`useragent_test.go:157` | 同构 |
| 16 | 二进制/常量：上游 base `copilot.tencent.com` / `www.codebuddy.cn` / `www.workbuddy.ai` | `src/upstream.ts:143-145` | `cmd/login/main.go:39-43` | 逐字 |

### 1.2 [1] 刻意**没有**借鉴的部分（= [2] 的账号池/网关能力）

`1-.../src/` 全部文件只有 20 个 ts/tsx，没有任何一个模块对应 [2] 的下列能力：

| [2] 的能力 | [2] 位置 | [1] 是否有 | 为什么没有（[1] 的设计前提） |
|---|---|---|---|
| 多账号池 / 加权随机选号 | `internal/pool/pick.go` | ❌ | [1] 只认"桌面 App 当前登录的这一个账号"，且**显式拒绝跨产品凭据混用**（`src/auth.ts:328-340`） |
| 账号冷却 / 退避 / 熔断 | `internal/pool/cooldown.go`、`degrade.go`、`state.go` | ❌ | 单账号谈冷却无意义 |
| session 粘性 / 会话 ID 复用 | `internal/session/session.go`、`ids.go` | ❌ | DSH 自己管会话，插件只做无状态转发 |
| realm（CN/国际）池隔离 | `internal/pool/realm.go` | 部分 | [1] 用**两个独立 provider**（`workbuddy` / `workbuddy-ai`，`src/variants.ts:54-81`）替代了"一个池里分 realm" |
| 定时任务：签到 / 猫猫旅行 / 活跃上报 / 保活 / 开学季 | `internal/scheduler/*.go`、`school.go`、`travel.go` | ❌ | [1] 是"用户在用 DSH 时才活着"的插件，没有常驻调度器 |
| SSE 重建 / 白名单清洗 / reasoning_content 回填 / tool 配对修复 | `internal/upstream/sse.go`、`sanitize.go`、`tool_pairing.go` | ❌ | [1] 后端是 pi-ai，SSE 由 `pi-ai` + DSH 消费，不做重写 |
| 系统提示词 passthrough/custom 模式 | `internal/prompt/prompt.go` | ❌ | 提示词由 DSH harness 全权所有（`src/index.ts:1-14` 明确"streaming, tool calls, compaction, permissions stay Harness-owned"） |
| 对下游暴露 `/v1` + 多密钥 + IP 管控 + 用量审计 | `internal/server/handler.go:105-121`、`wafip.go` | ❌ | [1] 的 `/v1` 只在 `127.0.0.1` 随机端口，且带**每进程随机共享密钥**（`src/shim.ts:116-130`），设计目标恰恰是"本地进程也不能白嫖" |
| Redis 持久化 / 跨实例状态 | `internal/redisstore/` | ❌ | 单机插件 |

**一句话**：`[1] 借用的是 [2] 逆向出来的"腾讯上游怎么打"；[2] 的"怎么运营一堆账号"完全没被借用。**

### 1.3 反向：借鉴之后已经漂移，以及 [1] 反超的地方（重要，决定了后续工作方向）

这部分是我在对照时发现的**双向不一致**，直接影响"要不要合、怎么合"：

**(a) [1] 落后于 [2] 的地方 —— 应当跟进的**

| 项 | [1] 现状 | [2] 现状 | 影响 |
|---|---|---|---|
| 刷新/目录请求的 UA | `CLI/2.63.2 CodeBuddy/2.63.2`（`src/upstream.ts:179`） | `WorkBuddy/5.5.4 WorkBuddy/5.5.4 CLI/2.137.1`，且测试里明写"旧值 `CLI/2.63.2 CodeBuddy/2.63.2` 已被对齐替换"（`useragent_test.go:166`、`headers.go:26`） | [1] 的 chat 用了新形状 UA，但 refresh/catalog 仍是旧 UA——**同一插件内两套身份**，可能是被风控识别的特征 |
| `X-Auth-Refresh-Source` | `'workbuddy'`（`src/upstream.ts:364`） | `'plugin'`（`headers.go:433`，注释说"对齐官方客户端 refresh 渠道标识"） | 值不一致 |
| 国际版积分端点 | 固定 `/v2/billing/meter/get-user-resource`（`src/upstream.ts:702`） | global 优先**无 `/v2`** 的 `/billing/meter/get-user-resource`，CN/失败才回退 `/v2`（`client.go:807-809`、global_e2e_test.go:248-271） | [1] 国际版积分可能取不到 |
| CN 模型目录 | 单路 `/console/enterprises/personal/models` | **双路并发合并**：`/console`（或 global `/v2/enterprises/personal/models`）+ `/v3/config`（`client.go:1198`、`handler_test.go:1256-1278`） | [1] 的 CN 目录字段比 [2] 少（尤其 v3 才有的 contextWindow / promotions） |
| 用量归属头 | 只有 `X-Product: SaaS` | `X-Product` / `X-IDE-Name` / `X-IDE-Type` / `X-IDE-Version` / `X-Agent-Purpose` 五头，且可配 `client_name=WorkBuddy`（`headers.go:337-356`） | [1] 在腾讯后台的"使用端"归因会显示成 SaaS 而非 WorkBuddy |
| N 种任务接口（签到/旅行/上报/保活） | 无 | `internal/upstream/report.go`、`travel.go`、`growth_bonus.go`、`growth_reward.go`、`trial.go` | [1] 不帮用户赚积分 |

**(b) [1] 反超 [2] 的地方 —— 值得回灌上游的**

| 项 | [1] 的实现 | [2] 的状态 |
|---|---|---|
| **CN 企业账号积分**：`POST /v2/billing/meter/get-enterprise-user-usage`，空 body、身份全走 header，并兼容 `limitNum/limit_num` + `credit/used_num` 两种拼写，`limitNum === -1` 表示"无上限" | `src/upstream.ts:768-831`（注释说是对着 `app.asar` 里的 `BackendProvider.getEnterpriseUsage` 核对的） | 搜索全仓**无** `enterprise-user-usage` —— [2] 未处理企业账号（[1] 把这类账号的积分读成 0 的问题修在 issue #31） |
| **限时促销 / 折扣**：`/v3/config` 的 `modelPromotions[]`（`enabled` + `modelIds` + `schedule.validFrom/validUntil` + `discount.factor/displayMode:"replace"` + `badge.label` + `priority`），并在**每次读取时按当前时间重算**，过期后不继续谎报"免费" | `src/upstream.ts:1019-1123` | 搜索全仓**无** `modelPromotions` |
| **上下文窗口多档**：`contextWindow.defaultLength` + `supportedLengths[]`，可让用户选"最大窗口" | `src/upstream.ts:988-995`、`src/status-paths.ts:130-153` | 搜索全仓**无** `defaultLength` |
| **推理档位探测（probe）**：对上游未声明 effort 集合的模型，实发一次流式请求验证哪些档位被接受，结果落盘并驱动 DSH 的模型档位选择器 | `src/probe.ts`、`src/probe-service.ts`、`src/probe-store.ts`（[2] 只有静态兜底表 `EffortListing`，`handler.go:338`） | 部分超出 |
| **`developer` → `system` 角色改写**：pi-ai 发 `role:"developer"`，上游以 400/11128 拒绝 | `src/upstream.ts:400-424` | [2] 无此需要（它自己构造 body） |

> 结论：**两个实现是"同一份协议知识的两条独立演进线"，已经互相不覆盖了。** 任何整合都必须先做一次协议层对齐（把 (a) 补齐、把 (b) 回灌），否则整合出来的插件会用着一份过期的线格式。

---

## 2. 问题一的延伸：[1] 的架构本质与"缝"在哪里

要判断"3、4 的能力能不能装进 1"，必须先看清 [1] 的结构和它的扩展点。

### 2.1 运行时结构（4 个部件）

| 部件 | 文件 | 职责 |
|---|---|---|
| **凭据层** | `src/auth.ts`（`WorkBuddyCredentialStore`） | 只读桌面 App 的 `workbuddy-desktop.info`；刷新结果写到 `$DSH_HOME/.workbuddy-auth.json`；**身份优先于过期时间**（`auth.ts:341-351`） |
| **上游客户端** | `src/upstream.ts`（`WorkBuddyUpstreamClient`） | chat / refresh / catalog / credits 四个调用，含全部线格式怪癖 |
| **loopback shim** | `src/shim.ts` | `127.0.0.1:0` 随机端口上的最小 OpenAI 兼容端点；四重加固（Host 必须 loopback、Origin 必须 loopback、`application/json`、`Authorization: Bearer <每进程随机密钥>`） |
| **pi-ai 适配器** | `src/adapter.ts`（`WorkBuddyPiAiAdapter extends PiAiAdapter`） | 把目录变成 pi-ai 模型描述子；只覆写 `listModels`/`resolveModel` 的显示名（拼上倍率与促销徽章） |
| **宿主插件** | `src/index.ts`（`apply(ctx, config)`） | 组装两个 variant、注册 provider、注册设置分区、凭据轮询、目录拉取与降级 |
| **浏览器半** | `src/client/*.tsx` | 设置页卡片（`WorkBuddyPluginCard.tsx`）+ 探测控件（`WorkBuddyProbeControl.tsx`），消费同源 status/probe 路由 |

### 2.2 四个可用的"缝"（后续所有整合都从这里进）

1. **同源 HTTP 路由缝**：`registerWorkBuddyStatusRoute` / `registerWorkBuddyProbeRoute`（`src/web-status.ts`、`src/probe-route.ts`），契约在 `src/status-paths.ts`。
   → 加"账号面板 / 用量统计 / 任务日志"等**只读展示**，走这里最省事：host 半多返回几个字段，浏览器半多渲染几个区块。契约文件已经是 host/浏览器共享的单一来源。

2. **上游客户端缝**：`WorkBuddyUpstreamClient` 把 base 硬编码为腾讯域名（`src/upstream.ts:143-145, 313-323`）。
   → 把它参数化成"可以指向任意 OpenAI 兼容 base + Bearer key"，就能让 DSH **直连 workbuddy2api 网关**（见路线 B）。这是**性价比最高的一处改动**。

3. **shim 缝**：shim 是唯一出口（`src/shim.ts:186-189`）。
   → 在此处**透明记录**每次请求的模型、耗时、首字延迟、token 用量，就能在插件内产出 [4] 那种"全量审计"数据，而不需要外部网关。

4. **UI 槽位缝**（**本节是 v2 新增的关键修正**）：DSH 的 Web UI 是一个**槽位（slot）系统**，插件通过 `ctx.slots.register(...)` 往命名槽位里塞 React 组件。DSH 官方设计意图就是"**功能自持其设置页**"，因此存在**一等公民的设置页槽位**。

#### 2.2.1 证据：槽位不是猜测，是从已发布包解出来的

`@deepseek-ai/dsh-client-ui-settings/lib/types/client/contract/slots.d.ts` 是设置域槽位的**权威契约**（该文件顶部注释原文）：

> "A feature owns its own settings pages — adding a setting never means editing the shell"

其中 `settings.section` 的定义（逐字）：

> "**One settings page per list entry.** Registrant options carry the nav identity: `id` (section key, drives `only` filtering), `order` (nav position), `label` (registrant-localized display text …). Sections render inside the panel content column."

即：**`settings.section` 的一个注册项 = DSH 设置面板里的一个独立导航页**，本插件自己提供导航标签与整列内容。

现成范例（DSH 自带的 Agent Preset 设置页），`@deepseek-ai/dsh-client-ui-agent-preset/lib/client.js:1806-1813`：

```js
ctx.slots.inject("settings.section", () => ctx.slots.register({
  name: "settings.section",
  id: "agent-presets",
  order: 20,
  label: () => ctx.locale.bind("settings.agentPreset")("nav"),
  locale: "settings.agentPreset",
  inject: sectionInjected
}, AgentPresetSection));
```

#### 2.2.2 已确认的设置域槽位清单（全部来自 `ui-settings` / `ui-settings-plugins` / `ui-conversation` 的契约产物）

| 槽位 | kind | 语义 | 对本项目的用途 |
|---|---|---|---|
| **`settings.section`** | list | **一个注册项 = 一个独立设置页**（自带导航项 `label`/`order`/`id`） | ★ **合并后控制台的主容器** |
| `settings.plugins.tab` | list | Plugins 设置区里的一个**标签页** | 账号/池配置页 |
| `settings.plugin.item` | list | Plugins 区里**单个插件的一张卡片** | ← **当前 [1] 用的就是它**（`client/index.tsx:77-89`） |
| `settings.general.item` | list | General 区里的一行偏好（如语言、外观） | 后端 base / key / 模式开关 |
| `settings.onboarding` | list | 引导步骤 | 首次配置向导（可选） |
| `settings.trigger` / `.header` / `.action` / `.close` | single/list | 设置面板外壳文字与动作位 | 一般不用碰 |

会话域另有（`ui-conversation` 契约）：`conversation.view`、`conversation.session.header.{actions,utilities,corner,leading,lineage}`、`conversation.composer.dock`、`conversation.input.{dock,overlay,right,left,model,attachments,permission,plan}`、`conversation.content`、`conversation.hero.*`。

→ **结论：用户的判断成立。** 当前 [1] 只把自己做成了 Plugins 区里的一张卡片（`settings.plugin.item`，最小表面积）；而 `settings.section` 允许把 [3]/[4] 的**展示类能力重写成一个完整的、有一级导航的 WorkBuddy 设置板块**——这正是"在 DSH 设置上新增一个独立的功能板块"。

#### 2.2.3 对 P1 起决定作用的两条实测事实

上一小节的范例取自 `dsh-client-ui-agent-preset`，而**那个包并不在 [1] 的依赖树里**。P1 真正要用的是下面这两条在 [1] 已安装依赖上复核过的事实：

**事实一：`settings.section` 不需要升 DSH 版本、不需要加依赖。**
它由 `@deepseek-ai/dsh-client-ui-settings-plugins@0.1.5-rc.1` 声明，而这个包**已经是 [1] 的 devDependency，且已在 `dsh.client.inject` 名单中**（`package.json`）。该版本的注册项形状与 0.1.6-alpha 完全一致：`{ name, id, order, label, locale, inject, children? }`。

**事实二：官方 Plugins 页自己就是一个 `settings.section`，它就是"总板块 + 内部分页"的官方样板。**
`dsh-client-ui-settings-plugins/lib/client.js:1761-1772`：

```js
ctx.slots.inject("settings.section", () => ctx.slots.register({
  name: "settings.section",
  id: "plugins",
  order: 15,
  label: () => t("nav"),
  locale: NS,
  inject: sectionInjected,
  children: { "settings.plugins.tab": { kind: "list", scope: "root" } },
}, PluginsSettingsSection));
```

两点直接可用：

1. `id`/`order`/`label`/`locale`/`inject` 就是本版本的实际字段名（注意与 `settings.plugin.item` 用的是 `key`/`priority` **不同名**，不能照搬）。
2. `children` 让一个板块**对外声明并托管子槽位**，子槽位由父组件用 `renderSlot` 渲染（`PluginsSettingsSection` 的 props 是 `PropsRuntime<'settings.section'> & PropsLocale<'settings.plugins'> & PropsRenderSlots<'settings.plugins.tab'>`）。

→ 但 WorkBuddy 的四个内部分页（账号与积分／模型中心／用量／测试台）**全部由本插件自己提供、不来自其他插件**，所以更简单的做法是：内部标签状态自持（一个 `useState`），只在需要外部插件往里塞东西时才用 `children`。原计划（§4 路线 A）里的"只注册一个 `settings.section`"不变，`children` 作为备选手段记下即可。

> **方法坑（我踩过一次）**：`grep node_modules/@deepseek-ai` 会**假阴性**——那些目录是 pnpm 符号链接，而指向的 `.pnpm` 实体目录名又被截断加哈希（如 `@deepseek-ai+dsh-client-ui-_98e99932…`）。唯一可靠的核验方式是读 `.pnpm/*/node_modules/@deepseek-ai/*/package.json` 拿真包名，见 `.probe/find-section-slot.mjs` 与 `.probe/dump-section-contract.mjs`。

### 2.3 两条硬约束（决定了很多能力"装不进来"）

**约束一：同源 + loopback，外站不可嵌入。**

浏览器半运行在 DSH 的 Web UI 里，受同源 + loopback 守卫（`src/shim.ts:151-168`）。

→ **不能 iframe 外部控制台**（[3] 的 Vite 页面 / [4] 的 Next.js 页面都会被 CSP/`frame-ancestors` + loopback Origin 检查挡掉）。
→ 所谓"把 3 和 4 的**展示**整合到 1"，**只能是"用插件的 React 组件重新实现有用的视图"**，数据由缝 1/3 提供；要么就只是"给个链接跳出去"。
→ 注意这与缝 4 并不矛盾：**容器是 DSH 原生的**（`settings.section` 是 DSH 自己的设置面板），被移入的是**我们自己写的 React 视图**，不是别人的页面。

**约束二：设置页不是"数据源"，只是"渲染位"。**

`settings.section` 只给出导航身份 + `close()` 一个外壳动作；数据仍必须经**自己注册的同源路由 + inject face** 拿到（契约原文：section 的数据 "arrives through its own inject faces and stores"）。
→ 所以顺序上 **缝 1（路由与契约）必须先于缝 4（页面）**；先有数据面，再谈搬到独立板块。
→ 另注：`label` 由注册方自己本地化（"the registrant re-registers with fresh text on locale change"），所以 i18n 从"可选加分项"变成**设置页的必需项**。

---

## 3. 问题二：[3]、[4] 的能力清单与"能否装进 [1]"判定

### 3.1 [3] workbuddy2api-gui 的能力（Go + React Vite）

路由清单（`3-.../internal/api/server.go:48-99`）：会话/登录/改密、overview、账号 CRUD+批量导入、单号 checkin/refresh/travel/credits、批量任务、扫码登录向导（start/status/poll/cancel）、模型列表、统计与重置、定价编辑、chat + chat/stream 测试台、config 读写与重置、系统信息与重启、静态资源。

前端页面（`3-.../web/src/pages/`）：`Accounts`、`ConfigPage`、`Dashboard`、`Login`、`LoginWizard`、`Playground`、`StatsPage`、`System`、`TaskProgress`、`TrendChart`。

特点：**代码量小、单页控制台、强依赖 workbuddy2api 运行中**（`internal/gateway/client.go` 打它的 API）、靠 docker 控制上游。

### 3.2 [4] workbuddy-manager 的能力（FastAPI + Next.js）

路由（`4-.../server/main.py:125-140`）：`auth`、`accounts`、`keys`、`logs`、`stats`、`security`、`settings`、`system`、`models`、`playground`、`gateway`、`anthropic`、`responses`。

前端页面（`4-.../web/app/(main)/`）：`dashboard`、`accounts`、`keys`、`logs`、`models`、`playground`、`security`、`settings`、`stats`、`tasks` + `(auth)/login`；i18n 5 语言（`web/lib/i18n/locales/{zh-CN,zh-TW,en,ja,ko}.json`）。

特色能力（README 功能一览）：
- **账号**：扫码纳管（成功后自动签到+落盘+重载上游）、Token 有效期进度条 + 「最后续期」时间、运行时状态与上游账号池状态合并展示（在线/冷却/禁用/过期）、**模型级限流单独标记**、积分余额（直连腾讯实时值 + `实时`/`缓存 Ns 前` 标注）、**积分到期倒计时**（按套餐分批过期）、**临时停用**（停用期不接对话但保活照常）、积分变动流水（靠余额比对兜住上游不打日志的渠道）
- **网关/密钥**：多密钥分发（每钥：限定版本 CN/global、有效期、最大 IP 数、IP 白名单、模型白名单、Token 配额）、密钥只存 SHA-256、模型别名映射、CN/国际一键切换（账号/模型/测试台/日志/统计全按版本过滤）、模型中心（显示名/描述/上下文/最大输出/**推理档位**/**积分倍率**/多模态/仅推理，按倍率排序，数据直连腾讯模型接口并**如实标注来源**）、聊天测试台（含思考强度与**实时消耗积分**）、入站 IP 白/黑名单（CIDR）、全量审计（密钥/IP/模型/状态码/**首字延迟**/总耗时/token/**实际扣费**）
- **设置**：四类定时任务独立开关 + 执行时刻、系统提示词 `passthrough`/`custom`、限流冷却基数与退避上限
- **运维**：一键更新上游、发布签名校验、docker 控制、安全审计文档

### 3.3 可行性判定矩阵

判定口径：**[1] 是"本机 · 单用户 · DSH 内部 provider"**。凡是需要"多租户/公网/常驻/多账号"的前提，都不能直接进来。

| [3]/[4] 的能力 | 能否进 [1] | 判定理由 | 若要进，代价 |
|---|---|---|---|
| 账号列表（多账号） | ⚠️ **可，但要先造前提** | [1] 现只认 1 个账号/variant（`auth.ts:321-352`）；多账号是 [2] 池能力的前置 | 新写本地账号库（目录扫描 `auths/*.json` + 多桌面 profile）+ 选号策略（移植 `pool/pick.go`+`cooldown.go`） |
| Token 有效期 / 最后续期展示 | ✅ **直接可做** | `auth.ts` 已有 `expiresAtMs`/`refreshExpiresAtMs`/`source`，status 契约已带 `expiresAt`（`status-paths.ts:171`） | 小：补「最后续期」字段 + 进度条组件 |
| 积分余额 + 实时/缓存标注 + 到期倒计时 | ✅ **可做** | `fetchCredits` 已返回 `accounts[]`（含 `size/remain`）与 `cycleResetTime`（`upstream.ts:104-128`） | 中：补每套餐 `PackageEndTimeRange` 的到期时刻（响应里已有 `CycleCapacity*`，需再解析到期字段）+ 倒计时组件 |
| 模型中心（倍率/档位/能力/排序） | ✅ **可做，且 [1] 已经更强** | `status-paths.ts:108-153` 的 `WorkBuddyWebModelBadge` 已有 credits/badges/free/rateUnknown/contextWindow/maxContextWindow | 小：板块加一个可排序表格 |
| 推理档位探测 | ✅ **[1] 独有** | `src/probe*.ts` 比 [2] 的静态表更强 | 无 |
| 聊天测试台 | ✅ **可做** | shim 已是 OpenAI 端点；组件内直接 fetch 同源 probe/chat | 中：需要一条受控的 host 侧试调路由（复用 probe 的鉴权模式） |
| 用量统计 / 全量审计（模型·耗时·首字延迟·token·扣费） | ✅ **可做** | shim 是所有流量的唯一出口（`shim.ts:186-189`），在此埋点即可；扣费可从 SSE 的 `usage.credit` 取 | 中：埋点 + 本地存储（`$DSH_HOME` 下的 jsonl，参照 `catalog-store.ts` 的原子写）+ 图表组件 |
| 临时停用 / 账号摘除 | ⚠️ 多账号后才有意义 | 同上 | 中 |
| 积分变动流水 | ⚠️ 可做但价值低 | [1] 不做签到/旅行，余额增长渠道少 | 小 |
| 模型别名映射（`gpt-4o-mini` → 实际模型） | ✅ **可做且很实用** | DSH 模型选择器按 `model.id` 路由（`adapter.ts:83-90`），插桩即可 | 小：目录层加一层别名映射表 |
| **定时任务**（签到/猫猫旅行/活跃上报/保活/开学季） | ❌ **不该进插件，或需征得同意后做** | [1] 无常驻进程，且这些**会真的花/赚积分**，属于账号运营行为，不是"接入模型" | 大：需移植 `scheduler/*.go` + 常驻定时器 + 明确的用户授权开关；**建议不做，或做成"可选·默认关"** |
| **多密钥分发 / 密钥哈希 / IP 白黑名单 / Token 配额** | ❌ **不能进，且无意义** | 需要"对外服务多租户"；[1] 的 `/v1` 只在 loopback，唯一消费者是 DSH 自己 | —— |
| **系统提示词 passthrough/custom** | ❌ **不能进** | DSH harness 拥有提示词（`index.ts:1-14`），插件改写会与 harness 的 compaction/tool 逻辑打架 | —— |
| CN/国际切换 | ✅ **已实现且更干净** | 双 provider + 双 variant（`variants.ts`），比 [4] 的"右上角全局切换态"更符合 DSH 的模型分组语义 | 无 |
| i18n 多语言 | ✅ **可做** | [1] 已有 `src/client/locales.ts`；可扩展到 [4] 的 5 语言 | 小 |
| 一键更新 / 发布签名 / docker 控制 / 安全审计 | ❌ **不该进** | 插件不该管宿主的部署与容器 | —— |
| 运营级仪表盘（健康度/冷却/有效期总览） | ⚠️ 多账号后才有意义 | 同"账号列表" | 中 |

**判定总结：**
- **可直接吸收的（吃掉 [4] 的"展示"精髓）**：账号/凭据状态面板（有效期+最后续期）、积分面板（实时/缓存标注+到期倒计时）、模型中心（倍率/档位/能力/排序/别名映射）、聊天测试台、用量与审计统计（含首字延迟与扣费）、i18n。
- **必须先造前提的**：多账号 —— 而这一步天然就要移植 [2] 的 `pool` 包逻辑，这才是"账号管理"进插件的正确姿势。
- **必须放弃的**：密钥分发、IP 管控、公网网关、提示词改写、docker/更新运维、定时任务（或至少默认关且强授权）。这些不是"漏做"，而是**架构上属于另一个进程/另一个产品**。

---

## 4. 整合方案

基于上面的判定，有两条**互补而非互斥**的路线。

### 路线 A：插件内自足（把 [4] 的"单账号运营面板"体验做进 [1]）

**目标**：不依赖任何外部服务，插件自身成为"DSH 用户能看到的全部 WorkBuddy 信息面"。

**UI 承载方式（v2 修正）**：不再把一切塞进 `settings.plugin.item` 那张卡片，而是**新增一个独立的 `settings.section` 设置页**作为主容器，卡片降级为"Plugins 区里的入口 + 概览"。

```
DSH 设置面板
├─ General                      （settings.general.item：后端 base / key / 模式开关）
├─ Agent Preset                 （DSH 自带）
├─ Plugins
│   └─ dsh-workbuddy-connect    （settings.plugin.item：保留现有卡片 = 概览 + 快捷开关）
└─ WorkBuddy                    （★ settings.section：新增的独立板块）
    ├─ 账号与凭据
    ├─ 积分与套餐（实时/缓存标注 + 到期倒计时）
    ├─ 模型中心（倍率 / 档位 / 上下文 / 能力 / 排序 + 别名映射）
    ├─ 用量与审计（调用数 / token / 首字延迟 / 扣费 / 按模型与按日）
    └─ 测试台（同源）
```

改动点（按性价比排序）：
1. **协议层对齐**（先做，无 UI 风险）
   - 统一出站 UA 为三段式（含 refresh/catalog 路径），参数化 CLI 版本默认值 → 对齐 [2] `headers.go:26`
   - `X-Auth-Refresh-Source` 改 `plugin`
   - 国际版积分改走无 `/v2` 的 billing 路径 + 回退（对齐 `client.go:807-809`）
   - CN 目录改双路合并（`/console` + `/v3/config`），并补 `X-Product`/`X-IDE-*`/`X-Agent-Purpose` 归因头
2. **凭据/账号面板**：status 契约扩字段（`lastRefreshedAt`、每套餐到期时刻），进度条 + 倒计时
3. **模型中心**：可排序表格（倍率 / 档位 / 上下文 / 能力标记）+ 别名映射设置
4. **用量与审计**：shim 埋点 → `$DSH_HOME/.workbuddy-usage.jsonl`（原子写 + 轮转）→ 用量页（调用数 / token / 首字延迟 / 总耗时 / 扣费 / 按模型与按日聚合）
5. **同源测试台**：复用 probe 的"同源 + 每进程密钥"模式，加一条 `POST /plugins/.../playground`
6. **可选多账号**（前置条件：本地账号目录 + 移植 `pool/pick.go`/`cooldown.go`）：目录可读 `auths/workbuddy*.json`（[4] 的格式，见 `4-.../server/services/wb2api.py` 的 `_safe_file`），轮询/冷却选出站账号，模型照旧对外暴露同一组 id
   - **这条打通后，[3]/[4] 的用户可以"复用同一个 `auths/` 目录"**，这是最自然的互操作点
7. i18n 扩到 5 语言（**由"加分项"升为"设置页必需"**：`settings.section` 的 `label` 由注册方自己本地化）

### 路线 B：插件远程池模式（让 DSH 直连 [2] 的账号池）

**目标**：把 `WorkBuddyUpstreamClient` 的 base 参数化，让用户二选一：
- 直连腾讯（现状，零依赖）
- 指向 `workbuddy2api` 实例（`WB2API_BASE` + `WB2API_KEY`），此时 DSH 拿到的是**整个账号池的吞吐与容错**，而 [3]/[4] 继续当它的控制台

**为什么这是"把 3 和 4 的能力整合到 1"的最优解**：账号池、密钥、IP、审计、定时任务的复杂度留在 Go/Python 服务里（[3]/[4] 已经做好），[1] 只做一个**一等公民客户端**——不重复实现，也不牺牲 DSH 侧的体验。

改动点：
- `upstream.ts` 的 `CN_CHAT_BASE`/`GLOBAL_BASE`/billing base 改为可注入配置（新增 `remote?: { base, key, modelPrefix? }`）
- shim 的凭据解析改为"remote 模式跳过本地 auth 文件，直接透传 key"
- variant 在 remote 模式下把 CN/global 都指到同一个 base，用 [2] 的 `provider/model` 前缀或模型别名区分（[2] `/v1/models` 带 realm 前缀）
- 板块在 remote 模式下改为展示 `GET /status` 的池信息（在线数/冷却数/积分），这是**真正把 [4] 仪表盘展示搬进 DSH** 且不违反 2.3 约束一的做法
- 保留 `probe` 只在直连模式可用（remote 模式下档位信息由 [2] 的 `/v1/models` 提供）

### 推荐：A + B 组合，B 优先

理由：
1. B 的改动面最小（一个 base 参数化 + 一个凭据分支），却直接把 [4] 最有价值的"账号池运营"能力引入 DSH，且**不与 [3]/[4] 竞争**，反而让它们更有用。
2. A 的协议对齐无论走不走 B 都必须做（否则线格式已经落后于 [2]）。
3. A 的展示类能力（积分倒计时、模型中心、审计统计）在 B 落地后仍有独立价值——用户在 remote 模式下也需要看到"我这次花了多少"。

---

## 5. 分阶段实施计划

| 阶段 | 内容 | 交付物 | 风险 |
|---|---|---|---|
| **P0**（已完成） | 协议层对齐（UA / refresh-source / 国际积分路径 / CN 双路目录 / 归因头）；给 [2] 提 PR 回灌 `get-enterprise-user-usage`、`modelPromotions`、`contextWindow.defaultLength` | [1] 的 4 个上游模块 diff + 对 [2] 的回灌 PR | 低（纯线格式，可用现有 vitest 覆盖） |
| **P1** | 缝 #4 骨架：注册 `settings.section`（`id: 'workbuddy'`、`label` 走 locale、`order` 与 DSH 自带页错开），把现有卡片内容迁入第一版板块；卡片保留为概览入口 | 新增 `src/client/WorkBuddySettingsSection.tsx` + `client/index.tsx` 增一处 register | 低（同一套槽位 API，已有 try/catch 降级先例） |
| **P2** | 缝 #2 参数化：`WorkBuddyUpstreamClient` 支持 remote base + mode 配置；shim 支持 remote 凭据分支；板块内新增"池模式"，展示 `GET /status` 的账号概览 | 新增 `src/remote-pool.ts`；`variants.ts` 加 `mode` 字段；status 契约扩 `pool` 段 | 中（凭据分支，需防"本地文件与 remote key 混用"） |
| **P3** | 缝 #3 埋点：shim 记录 per-request 指标 → jsonl；板块加"用量"页（调用数/token/首字延迟/扣费/按模型与按日） | `src/usage-store.ts` + `WorkBuddyUsagePanel.tsx` | 中（本地存储增长与轮转） |
| **P4** | 展示补齐：[4] 的积分实时/缓存标注 + 套餐到期倒计时；模型中心表格（倍率/档位/上下文/能力/排序）+ 模型别名映射；i18n 扩 5 语言 | 板块各子页 + `locales.ts` 扩展 | 低 |
| **P5** | 同源测试台（复用 probe 鉴权模式） | `src/playground-route.ts` + 组件 | 中（需防被本地页面盗用，沿用 loopback+密钥模式即可） |
| **P6** | 可选多账号（本地 `auths/*.json` 目录 + 移植 `pool/pick.go`/`cooldown.go` 策略） | `src/account-pool.ts` | **高**（多账号 = 多份凭据与积分，误用会串号；[1] 已有"跨产品凭据拒绝"的良好先例可循，`auth.ts:328-340`） |
| **P7（不做/暂缓）** | 定时任务、密钥分发、IP 管控、提示词改写、部署运维 | —— | 属 [2]/[3]/[4] 或需强授权，不建议进插件 |

### 5.1 P0 实施记录（已完成）

全部落在 [1] 的 `src/upstream.ts`，无新增模块；两个 tsconfig 类型检查、vitest、tsdown 构建均已通过。

| 变更 | 对齐对象（[2] 的证据） | 说明 |
|---|---|---|
| 出站 UA 统一为桌面三段式 | `headers.go:58-72` `defaultWorkBuddyUAFor` | `WorkBuddy/<ver> <platform>/<ver> CLI/<cliVer>`，platform 段按 realm 切 `WorkBuddy`／`WorkBuddy AI`。原先 refresh 与 CN 目录用 `CLI/2.63.2 CodeBuddy/2.63.2`、国际目录用 `WorkBuddyAI/<ver>`，三套 UA 并存已消除 |
| `X-Auth-Refresh-Source: plugin` | `headers.go:432-433`（[2] 明记原值 `workbuddy` 与官方不一致，其内部编号 D3） | 一个常量修正 |
| 归因头组 | `headers.go:345-356` | chat 路径加 `X-Agent-Purpose: conversation` + `X-IDE-Name/Type/Product: WorkBuddy` + `X-IDE-Version`（与 UA 同源解析，不可能自称不同版本）。原 `X-Product: SaaS` 正是[2] 要消除的「网关特征」——[2] 保留它仅为显式回归旧行为 |
| `X-CodeBuddy-Request: 1` + `Accept-Language` | `headers.go:164-168`（D1 / D5） | 全出站路径（chat／refresh／目录／billing）都带。`Accept` 同时收紧为 `application/json`，chat 覆写为 `application/json, text/event-stream`（D6） |
| 国际积分路径 | `client.go:807-816` `billingMeterPaths` | global 首选无 `/v2` 的 `/billing/meter/get-user-resource`，**仅 404 时**回落 `/v2/...`；CN 保持单路 `/v2/...`（零回归）。非 404 错误不重试——401／积分尽必须按原样报出 |
| 目录双路 | `client.go:1155-1157, 1203-1279` | `/v3/config` + 企业端点并发探测，单路失败不拖累另一路。企业路径按 realm 分：CN `/console/...`、global `/v2/...`（[2] 实测 global 的 `/console` 返 500） |
| `/v3/config` 解析口径 | `client.go:1356-1402` | 新增 `parseV3Catalog`：文档有 `cli` 名单则按其投影（global 实测有），无则取全量 `models` 并剔除非对话条目。非对话规则移植自 [2] `nonChatModel`（`client.go:1176-1190`：`nes-/completion-/codewise-` 前缀、`maxOutputTokens ≤ 256`、`tags` 含 `text-to-image`）——**没有这条规则，CN 的 v3 面会把补全／生图模型灌进对话模型选择器** |

**两处刻意偏离 [2]，都取「零回归」方向**：

1. **合并主从按 realm 定**：[2] 恒以 `/v3/config` 为主（`client.go:1230` `mergeModelInfos(v3, enterprise)`）。[1] 改为 global 以 v3 为主、CN 以企业端点为主——CN 的企业文档正是官方 CLI 自己消费的那份，是 [1] 已验证的契约；[2] 需要 v3 当主，是因为它的企业端点不可靠。
2. **`parseModelCatalog` 仍要求 `cli` 名单**：只放宽 v3 面。企业文档没有 `cli` 名单时依旧是硬错误——[1] 原有的「空目录必须显形」约定（issue #31 的教训：别把「没读懂」渲染成看起来合理的数字）保留。

**顺带发现（与本次改动无关，未修）**：

- `processStartTimeMs` 的 Windows 分支调用 `wmic`（`src/host-heartbeat.ts:114-127`），而 `wmic` 自 Windows 11 24H2 起已被移除；本机（10.0.26200）已确认不存在。后果：该函数在现行 Windows 上恒返 `undefined`，心跳的「PID 被复用」检测实际失效，退回纯 PID 存活判断。修法是把 `wmic process ... get CreationDate` 换成 `powershell -c "(Get-CimInstance Win32_Process -Filter 'ProcessId=<pid>').CreationDate"`。
- `tests/auth.spec.ts:372` 的 WSL 用例在 Windows 宿主上恒失败：`tmpdir()` 带盘符，WSL 翻译**正确**产出 `/mnt/d/...`，而断言用宿主 `join` 拼出反斜杠路径。该用例需要 POSIX 的 `tmpdir()` 才能通过。改动前的基线同样是这 2 条失败，与 P0 无关。

**明确不做（推迟）**：`X-Machine-ID` / `X-Session-ID`（[2] `headers.go:144-150` 按 `uid` 稳定派生）。那是「多账号共用一个网关出口」才需要的设备指纹；[1] 是单用户本地插件，无多号关联可隐藏，加它只增加一份需要跨重启稳定的派生状态。若 P6 做多账号，应连同一起做。

---

## 6. 风险与红线（必须写进设计的）

1. **凭据绝不外流**：[1] 现有的两条红线要保留并扩展——chat 请求不带 refresh token（`upstream.ts:348`）、每进程随机共享密钥不进上游（`shim.ts:112-116`）。remote 模式下新增红线：本地 auth 文件的 token 与 remote key **不得同时出现在同一个请求**。
2. **多账号 = 多身份**：一旦支持多账号，`X-User-Id`/`X-Enterprise-Id` 必须与实际选中的账号严格绑定；参照 `auth.ts:341-351` 的"身份优先于过期时间"原则，禁止按时间戳挑 token。
3. **合规**：四个项目都声明"仅供个人学习研究"。整合后的插件若引入定时任务/多账号池，性质从"接入模型"偏向"账号运营"，需要用户显式授权开关（[1] 已有先例：`probeConsent` 默认 false，`index.ts:220-234`）。
4. **不要重复实现 [4]**：[4] 的价值在"多租户运营"，插件里重做一遍既跑不动也没人用。定位应该是"DSH 内的单用户视图 + [2] 池的一等客户端"。
5. **协议漂移是真实风险**：本文 1.3 已列出 6 项 [1]→[2] 的滞后。建议把 [2] 的线格式常量作为单一事实源，用一份**共享的协议快照**（端点/头/关键词表/UA 形状）驱动 [1] 的实现，并加一致性测试。

---

## 7. 一句话总结

- **[1] 从 [2] 借的是"腾讯上游线格式"**（端点、请求体怪数、`X-No-*` 头族、积分关键词表、模型目录字段），**没借账号池/网关**，而且两边已经互相漂移（[1] 落后 6 项，反超 5 项）。
- **[3][4] 是 [2] 的控制台，不是独立能力源**；它们的能力中，**展示类（账号状态、积分倒计时、模型中心、用量审计、测试台）应该重写进 [1]，运营类（多密钥/IP/公网网关/定时任务/部署）在插件形态下不该进来**。
- **UI 承载已确认可行且被官方设计所鼓励**：DSH 有 `settings.section` 槽位，一个注册项就是一个**带一级导航的独立设置页**（契约原文 "One settings page per list entry"、"A feature owns its own settings pages"）。所以目标形态是**在 DSH 设置里新增一个 WorkBuddy 独立板块**，而不是只往 Plugins 区塞一张卡片（当前 [1] 用的是 `settings.plugin.item`，那是最小表面积的做法）。限制只有一条：**页面必须是自研 React 视图 + 自己的同源路由供数**，外站控制台无法 iframe。
- **最高性价比的整合点是"让 [1] 变成 [2] 的一等客户端"**（缝 #2：把上游 base 参数化），此时 [3]/[4] 继续做控制台、DSH 拿到账号池的吞吐与容错，三者的能力边界互不重叠。
- 无论选哪条路线，**P0 的协议对齐都是前置**。
