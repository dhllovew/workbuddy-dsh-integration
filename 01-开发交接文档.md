# WorkBuddy × DSH 插件整合 — 开发交接文档

> 面向「全新对话、零上下文」的接手者。读完这份应该能直接上手改代码，不需要重做分析。
> 配套文档：`00-总览与整合方案.md`（完整分析报告，含槽位证据与能力判定矩阵）。本文是**执行向**的摘要与状态快照。
> **与用户交流请用中文**（用户全程中文，且明确要求按实际逻辑写文档）。

---

## 1. 任务是什么

把四个仓库里两个「控制台」项目的有用能力，合并进那个 DSH 插件，做成 **DSH 设置里的一个独立 WorkBuddy 功能板块**。

### 1.1 用户已经拍定的决策（不要再问、不要再改）

| # | 决策 | 含义 |
|---|---|---|
| D1 | **一个总板块 + 内部分页** | 只注册**一个** `settings.section`（`id: 'workbuddy'`），内部用标签页分「账号与积分 / 模型中心 / 用量 / 测试台」。**不是**注册多个并列顶级板块 |
| D2 | **目标是原生板块，不是一张卡片** | DSH 的 Web UI 可改，所以要做成「设置里有独立导航项的功能板块」，而不是只在 Plugins 区塞卡片。现有卡片**保留**为 Plugins 概览入口 |
| D3 | **CN 优先实测** | 优先用国内账号做活上游实测 |
| D4 | **国际/企业账号：实现 + 单测，推迟活测** | 注意是**推迟验证**，不是不做功能。代码要写完并标注「未经活上游验证」 |
| D5 | **外站控制台不得 iframe** | 见 §5 硬约束。所有 UI 必须自研，数据走插件自己的同源路由 |
| D6 | **不做（P7）** | 定时任务、密钥分发、IP 管控、提示词改写、部署运维 —— 属 [2]/[3]/[4] 或需强授权，不进插件 |

### 1.2 四个仓库的角色（别搞混）

| 目录 | 技术栈 | 角色 |
|---|---|---|
| `1-dsh-workbuddy-connect` | TypeScript（DSH 插件，host 半 + React 浏览器半） | ★ **整合目标**，所有开发都在这里 |
| `2-workbuddy2api` | Go | **协议权威**（上游线格式的唯一事实源）+ OpenAI 兼容账号池网关 |
| `3-workbuddy2api-gui` | Go + React/Vite | [2] 的控制台（移植 `python/sdk` 上游） |
| `4-workbuddy-manager` | FastAPI + Next.js | [2] 的运营控制台（`docker-compose.yml:97` 把 `../workbuddy2api` 挂进容器） |

关键结论（已分析完，不必重做）：
- [1] **只从 [2] 借了「腾讯上游线格式」**（端点、请求体怪数、`X-No-*` 头族、积分关键词表、模型目录字段），**没借账号池/网关**。两边已互相漂移（[1] 落后 6 项、反超 5 项）。
- [3]/[4] **是 [2] 的控制台，不是独立能力源**。其展示类能力（账号状态、积分倒计时、模型中心、用量审计、测试台）应重写进 [1]；运营类能力不该进来。

---

## 2. 当前状态速览

| 阶段 | 状态 |
|---|---|
| P0 协议对齐 | ✅ **已完成并验证**（详见 §4） |
| P1 独立设置板块骨架 | ⬜ **下一步，未动工** |
| P2 远程池模式（缝 #2） | ⬜ 未开始 |
| P3 用量埋点（缝 #3） | ⬜ 未开始 |
| P4 展示补齐 | ⬜ 未开始 |
| P5 同源测试台 | ⬜ 未开始 |
| P6 可选多账号 | ⬜ 未开始 |

**现场残留状态（接手者必须知道）**：
- 插件已作为 `link:` 装进用户**真实的** `web` profile：`C:\Users\haishi\.dsh\profiles\web`（deps 里有 `dsh-workbuddy-connect`，`dsh.profile.bundles` 也已含它）。**这是唯一一份安装** —— 曾用于验证的隔离 profile `wbtest` 已删除，**不要再建重复安装**（用户明确要求全局只保留一份）。
- 用户的 `DSH_HOME\settings.yaml` 里 `agent-default-model` 就是 `provider: workbuddy`、`model: hy4-preview` —— **用户在真实使用这个 provider，别弄坏 `web` profile**。
- **web 服务正在跑**：PID 10620 监听 `127.0.0.1:3099`；Chrome（headless，由 js-reverse MCP 管理）已连着它。
- 重建/重启见 §3.3。

---

## 3. 开发环境与工具

### 3.1 事实清单

| 项 | 值 |
|---|---|
| 工作目录 | `D:\project\workbuddy反代实现分析` |
| 插件仓库 | `D:\project\workbuddy反代实现分析\1-dsh-workbuddy-connect` |
| `DSH_HOME` | `C:\Users\haishi\.dsh`（profiles 在 `.\profiles\`） |
| DSH CLI | `dsh`（`C:\nvm4w\nodejs\dsh.cmd`），版本 **0.1.5-rc.2** |
| 插件依赖 | `pnpm`，已 `pnpm install --frozen-lockfile` 装好（213 包，`@deepseek-ai/*` 为 0.1.5-rc.1） |
| 浏览器 | Chrome，经 `js-reverse` MCP（`launch_browser` / `new_page` / `take_screenshot` / `evaluate_script` / `get_console_logs`） |

### 3.2 本机 shell 的坑（**踩过，务必避开**）

1. **Cmd 工具会破坏带引号的路径**：`dir "C:\Program Files\x"` 报「文件名、目录名或卷标语法不正确」。对策：用 8.3 短路径（`C:\PROGRA~1`、`D:\PROGRA~1`），或改用 Read/Glob/Grep 工具。
2. **Windows PowerShell 5.1 按 ANSI 解码 `.ps1`**：脚本里的中文路径会变乱码（`反代实现分析` → `鍙嶄唬瀹炵幇鍒嗘瀽`）。
   对策：**脚本正文只用 ASCII**；路径靠「先在 cmd 里 `cd` 好 + 脚本内用相对路径」。
3. **本机找不到 `pwsh`（PowerShell 7）**：不在 PATH，`C:\Program Files` 与 `D:\Program Files` 递归搜索无 `pwsh.exe`，不在 `%LOCALAPPDATA%\Microsoft\WindowsApps`，也不在 `HKCU\Environment` 的 Path。本环境专用 PowerShell 工具按文档是 **5.1**。用户自称装了 PS7 —— **若用户给出 pwsh 全路径，就固定用 `pwsh.exe`**。
4. **工具调用里不能用 `timeout /t`**（非交互控制台下会挂住，并连带被工具超时杀掉）。要等待用 `ping -n N 127.0.0.1 > nul`。
5. **脱离式启动长驻进程**用：
   `Start-Process -FilePath cmd.exe -WindowStyle Hidden -ArgumentList '/c', '<带重定向的完整命令>'`
   （直接 `start` 起的进程会随工具调用超时一起被杀）
6. **`%TEMP%` 实际是 `D:\Personal\Temp`**（非默认位置）。
7. **写 memory 被系统拒绝**（`memory/v1/user/topics` Access denied），别在这上面浪费时间，把环境知识写进文档。

### 3.3 真实 UI 验证闭环（**这是本项目最重要的验收手段**）

单元测试**无法**覆盖真实槽位 API（client 半边 import 的是浏览器专用包，Node 测试环境加载不了；`tests/client-fallback.spec.ts` 只是**镜像**了 `apply()` 的形状）。所以真实 UI 走查不可省。

```bash
# 1) 把插件装进 profile（本地目录 -> link:）
dsh plugin --profile web add D:\project\workbuddy反代实现分析\1-dsh-workbuddy-connect

# 2) 启动 web（注意：--profile 不能叠在 web 子命令后面！）
#    要跑自定义 profile，直接给它传 app 参数：
dsh --profile web --no-open --port 3099      # 或 dsh web --no-open --port 3099
隔离 profile 的写法（**需先用 `dsh --profile <名> --from-default-profile web --dump-config` 创建**）：`dsh --profile <名> --no-open --port 3099`

# 3) 日志里会打印带 token 的访问地址，形如：
#    dsh web: http://127.0.0.1:3099/?token=XXXXXXXX
```

然后用浏览器工具：`launch_browser`(chrome, headless) → `new_page(<带 token 的 URL>)` → `take_screenshot(filePath)` → 用 Read 读图判读。
排查客户端加载失败要看 `get_console_logs`（`apply()` 失败会打印 `[dsh-workbuddy-connect] client card failed to load`）。

**已验证**：`link:` 安装**不会**引发 cordis 双实例问题（插件自带 node_modules 也照常工作）；两张卡片在真实 UI 正确渲染、中文文案正确、无控制台报错。
辅助脚本：`.probe\boot-web.ps1`、`.probe\boot-web-real.ps1`、`.probe\kill-port.ps1`（`powershell -NoProfile -File <脚本>` 调用）。

### 3.4 命令速查

```bash
cd D:\project\workbuddy反代实现分析\1-dsh-workbuddy-connect
npx tsc -p tsconfig.json          # host 半：只查 src（排除 src/client）+ 大部分 tests
npx tsc -p tsconfig.client.json   # 浏览器半：src/client + 5 个 client 测试
npx vitest run                    # 全部单测
npx tsdown                        # 构建（输出 lib/）
pnpm run check                    # typecheck && test && build（= 完整门）
```

**基线（改动前就已如此，不是回归）**：343 个用例，**341 通过 / 2 失败**。两个失败都是**平台条件**问题：
- `tests/auth.spec.ts` →「uses translated WSL environment paths when the Windows user differs」：Windows 宿主上 `tmpdir()` 带盘符 `D:\`，WSL 翻译**正确**产出 `/mnt/d/...`，而断言用宿主 `join` 拼出反斜杠路径。该用例需要 POSIX 的 `tmpdir()` 才能过。
- `tests/host-heartbeat.spec.ts` →「detects a recycled PID as dead」：见 §7 的 `wmic` 缺陷。

### 3.5 工程约定（**不知道会白费力气**）

1. **仓库是 CRLF 行尾**（`src/**`、`tests/**` 均为 CRLF，loneLF=0；而 `*.md` 是 LF）。用 Edit 工具时 `old_string` / `new_string` **必须带 `\r\n`**，否则 `old_string not found`，且原因非常难猜。`.probe\eol.mjs` 可查某文件的行尾。
2. **只能用 `pnpm install --frozen-lockfile`**。`@deepseek-ai/*` 的 npm `latest` dist-tag 指向旧的 `0.0.1-rc.x`，裸 `npm install` 会装错版本。
3. **两套 tsconfig 是分区检查的**：新增 **client 侧测试文件必须加进 `tsconfig.client.json` 的 `include`**，且**不要**加进 `tsconfig.json`（后者显式 `exclude` 了 `src/client` 与那 5 个 client 测试）。
4. **`apply()` 的 try/catch 形状在 `tests/client-fallback.spec.ts` 里被镜像复制**。改了 `src/client/index.tsx` 的 guarded body 或 `console.error` 文案，**必须同步改那个 spec**，否则 fallback 测试会静默偏离真实实现（这是仓库里原有的显式告警注释）。
5. 注释风格：解释**为什么**（尤其是「为什么不是更直白的写法」），不解释显而易见的行为。既有中文注释（安全红线类）保留。

---

## 4. P0 已完成的内容（协议对齐）

**改动文件（仅这些）**：
- `src/upstream.ts`（唯一的源码改动）
- `tests/chat-identity-wire.spec.ts`、`tests/variants.spec.ts`、`tests/catalog-lifecycle.spec.ts`
- 分析报告 `00-总览与整合方案.md`（§2.2.3 新增、§5.1 新增、§0/§4/§7 更新）

**逐项对齐 [2] 的成果**（每行都注明 [2] 里的依据）：

| 变更 | [2] 的依据 | 说明 |
|---|---|---|
| 出站 UA 统一为桌面三段式 | `internal/upstream/headers.go:58-72` | `WorkBuddy/<ver> <platform>/<ver> CLI/<cliVer>`，platform 段按 realm 切 `WorkBuddy` / `WorkBuddy AI`。原先**三套 UA 并存**（refresh+CN 目录用 `CLI/2.63.2 CodeBuddy/2.63.2`、国际目录用 `WorkBuddyAI/<ver>`）已消除 |
| `X-Auth-Refresh-Source: plugin` | `headers.go:432-433` | 常量修正（原值 `workbuddy` 与官方不一致，[2] 内部编号 D3） |
| 归因头组 | `headers.go:345-356` | chat 路径加 `X-Agent-Purpose: conversation` + `X-IDE-Name/Type/Product: WorkBuddy` + `X-IDE-Version`（与 UA **同源解析**，不会自称不同版本）。原 `X-Product: SaaS` 正是 [2] 要消除的「网关特征」 |
| `X-CodeBuddy-Request: 1` + `Accept-Language` | `headers.go:164-168`（D1/D5） | 全出站路径（chat/refresh/目录/billing）都带。`Accept` 收紧为 `application/json`，chat 覆写 `application/json, text/event-stream`（D6） |
| 国际积分路径 | `client.go:807-816` `billingMeterPaths` | global 首选无 `/v2` 的 `/billing/meter/get-user-resource`，**仅 404 时**回落 `/v2/...`；CN 保持单路 `/v2/...`（零回归）。非 404 不重试（401/积分尽必须原样报出） |
| 目录双路探测 | `client.go:1155-1157, 1203-1279` | `/v3/config` + 企业端点**并发**探测，单路失败不拖累另一路。企业路径按 realm：CN `/console/enterprises/personal/models`、global `/v2/enterprises/personal/models`（[2] 实测 global 的 `/console` 返 500） |
| `/v3/config` 解析口径 | `client.go:1356-1402` | 新增 `parseV3Catalog`：文档有 `cli` 名单则按其投影（global 实测有），无则取全量 `models` 并剔除非对话条目。**非对话规则移植自 [2] `nonChatModel`**（`client.go:1176-1190`：`nes-/completion-/codewise-` 前缀、`maxOutputTokens ≤ 256`、`tags` 含 `text-to-image`）——**没有它，CN 的 v3 面会把补全/生图模型灌进对话模型选择器** |

**两处刻意偏离 [2]，都取「零回归」方向**（改动者别当成 bug 顺手"修"回去）：
1. **合并主从按 realm 定**：[2] 恒以 `/v3/config` 为主（`client.go:1230`）。[1] 改为 global 以 v3 为主、**CN 以企业端点为主** —— CN 的企业文档正是官方 CLI 自己消费的那份，是 [1] 已验证的契约；[2] 需要 v3 当主是因为它的企业端点不可靠。
2. **`parseModelCatalog` 仍严格要求 `cli` 名单**：只放宽 v3 面。企业文档没有 `cli` 名单时依旧是硬错误 —— 保留 [1] 原有的「空目录必须显形」约定（issue #31 的教训：别把"没读懂"渲染成看似合理的数字）。

**P0 明确推迟的**：`X-Machine-ID` / `X-Session-ID`（[2] `headers.go:144-150` 按 `uid` 稳定派生）。那是「多账号共用一个网关出口」才需要的设备指纹；[1] 是单用户本地插件，无多号关联可隐藏。**若做 P6 多账号，应连同一起做**。

---

## 5. 已核实的关键技术事实（不用再查）

### 5.1 `settings.section` —— P1 的机制（**已实测，非推测**）

- 它由 **`@deepseek-ai/dsh-client-ui-settings-plugins`** 声明，而这个包**已经是 [1] 的 devDependency，且已在 `package.json` 的 `dsh.client.inject` 名单里**。
  → **P1 不需要升 DSH 版本、不需要加依赖。**
- **注册项形状**：`{ name, id, order, label, locale, inject, children? }`
  ⚠️ 注意与 `settings.plugin.item` 用的 **`key` / `priority` 不同名**（[1] 现有的卡片注册用的是后者），**不能照搬**。
- **官方样板**（`dsh-client-ui-settings-plugins/lib/client.js:1761-1772`）：官方 Plugins 页自己就是一个 section：
  ```js
  ctx.slots.inject("settings.section", () => ctx.slots.register({
    name: "settings.section",
    id: "plugins",
    order: 15,
    label: () => t("nav"),
    locale: NS,
    inject: sectionInjected,
    children: { "settings.plugins.tab": { kind: "list", scope: "root" } },
  }, PluginsSettingsSection));
  ```
  - `children` 让板块**对外声明并托管子槽位**，由父组件用 `renderSlot` 渲染（`PluginsSettingsSection` 的 props 是 `PropsRuntime<'settings.section'> & PropsLocale<'settings.plugins'> & PropsRenderSlots<'settings.plugins.tab'>`）。
  - **但 WorkBuddy 的四个内部分页全由本插件自供给、不来自别的插件**，所以更简单：标签状态自持（一个 `useState`）。`children` 留作备选。
- **真实 UI 已实证**：设置左侧导航实际是「通用设置 / 模型 / 插件 / Agent 预设」，第 4 项（`id: 'agent-presets'`, `order: 20`）就是 section 的实例。**`order` 建议用 25**（排在 Agent 预设之后）。

### 5.2 探查槽位时的致命方法坑（**我踩过一次**）

`grep node_modules/@deepseek-ai` 会**假阴性**：那些目录是 pnpm 符号链接，而指向的 `.pnpm` 实体目录名被**截断并加哈希**（如 `@deepseek-ai+dsh-client-ui-_98e99932...`）。
唯一可靠的核验方式：读 `.pnpm/*/node_modules/@deepseek-ai/*/package.json` 拿真包名。
现成脚本：`.probe\find-section-slot.mjs`、`.probe\dump-section-contract.mjs`。

### 5.3 两条硬约束

1. **同源 + loopback 守卫**（`src/shim.ts:151-168`）：**外站控制台（[3] 的 Vite / [4] 的 Next.js）无法 iframe 进 DSH**。所有合并来的 UI 必须是**自研 React 视图 + 插件自己的同源路由供数**。
2. **`settings.section` 只是渲染点，不是数据源**：数据必须来自已注册的同源路由 + `inject` face。
   → **缝 #1（路由契约）必须先于缝 #4（页面）**。另：`label` 要求注册方**自己本地化**，所以 i18n 是必需项而非可选项。

### 5.4 会话域也有的槽位（P5/测试台可能用到）

`conversation.view`、`conversation.session.header.{actions,utilities,corner,leading,lineage}`、`conversation.composer.dock`、`conversation.input.{dock,overlay,right,left,model,attachments,permission,plan}`、`conversation.content`、`conversation.hero.*`。
[1] 现在已在用 `conversation.input.right`（探针控件，`src/client/index.tsx:78-88`）。

---

## 6. 待办：P1–P6（含推迟活测清单）

### P1 独立设置板块骨架（下一步）

- 新增 `src/client/WorkBuddySettingsSection.tsx`
- `src/client/index.tsx` 增**一处** `settings.section` 注册：`id: 'workbuddy'`、`order: 25`、`label: () => t('nav')`、`locale: 'settings.workbuddy'`
- `src/client/locales.ts` 加 `nav` 等新键（命名空间 `settings.workbuddy` 已存在）
- 四个内部分页骨架：账号与积分 / 模型中心 / 用量 / 测试台（标签状态自持）
- 两张现有卡片**保留**（`settings.plugin.item`，`src/client/index.tsx:69-76`）作为 Plugins 概览入口
- 验收：§3.4 四关 + 真实 UI 截图（设置里出现第 5 个导航项「WorkBuddy」）
- ⚠️ 改 `apply()` guarded body 要**同步改** `tests/client-fallback.spec.ts`（§3.5 第 4 条）

### P2 缝 #2：远程池模式

`WorkBuddyUpstreamClient` 支持 remote base + mode；新增 `src/remote-pool.ts`；`variants.ts` 加 `mode`；status 契约扩 `pool` 段；板块加「池模式」页展示 `GET /status` 账号概览。
凭据红线：**本地 auth 文件与 remote key 不得同时出现在同一请求**。验收：单测 + UI + **CN 实测可连本机 [2] 网关**。

### P3 缝 #3：用量埋点

shim 记录 per-request 指标 → jsonl（注意本地存储增长与轮转）：`src/usage-store.ts` + 「用量」页（调用数/token/首字延迟/扣费，按模型与按日）。

### P4 展示补齐

[4] 的积分实时/缓存标注 + 套餐到期倒计时；模型中心表格（倍率/档位/上下文/能力/排序）+ 模型别名映射；i18n 扩语言。

### P5 同源测试台

`src/playground-route.ts` + 组件，**复用 probe 的 loopback + 每进程随机密钥模式**（`src/shim.ts:112-116`）。

### P6 可选多账号（高风险）

`src/account-pool.ts`，移植 [2] 的 `pool/pick.go` / `cooldown.go`；**一并补 `X-Machine-ID` / `X-Session-ID`**。
红线：`X-User-Id` / `X-Enterprise-Id` 必须与实际选中账号严格绑定；参照 `src/auth.ts:341-351` 的「身份优先于过期时间」原则，**禁止按时间戳挑 token**。需显式授权开关（先例：`probeConsent` 默认 false，`src/index.ts:220-234`）。

### 推迟「活上游实测」的具体清单（代码已写、单测已覆盖，只等账号）

1. 国际积分路径 `/billing/meter/get-user-resource` 的无 `/v2` 优先 + 404 回落
2. 国际目录新路由 `/v2/enterprises/personal/models`
3. **`/v3/config` 改用桌面三段式 UA（风险最高）**：依据是 [2] 的实测记录「三段式 UA 可过、浏览器 UA 返 400 code 12403」，**我无法自己打活上游验证**
4. 企业积分端点 `/v2/billing/meter/get-enterprise-user-usage` 与 CN 的 v3 补充路

**接手者义务**：这些点的代码注释与报告里已标注「按 [2] 实测记录实现，未经活上游验证」，请保持该标注，别让后人误认为已验证。

---

## 7. 已知问题（与整合任务无关，未修）

1. **`processStartTimeMs` 在现行 Windows 上恒失效**（**真实缺陷**）
   `src/host-heartbeat.ts:114-127` 的 win32 分支调用 `wmic`，而 `wmic` 自 Windows 11 24H2 起已被移除；本机（10.0.26200）已确认不存在。
   后果：该函数恒返 `undefined`，心跳的「PID 被复用」检测**实际不生效**，退回纯 PID 存活判断。
   修法：`wmic process where processid=<pid> get CreationDate` → `powershell -c "(Get-CimInstance Win32_Process -Filter 'ProcessId=<pid>').CreationDate"`。
   修完 `tests/host-heartbeat.spec.ts` 那条用例在本机就能过（另一条 WSL 用例仍需 POSIX 环境）。
2. WSL 路径用例在 Windows 恒失败 —— 见 §3.4，属测试夹具平台依赖。
3. 写 memory 被系统拒绝 —— 见 §3.2 第 7 条。

---

## 8. 文件索引

| 路径 | 内容 |
|---|---|
| `00-总览与整合方案.md` | 完整分析报告。**§2.2.1–2.2.3** 槽位证据、**§2.3** 硬约束、**§3.3** 能力判定矩阵、**§4** 路线 A/B、**§5** 阶段表、**§5.1 P0 实施记录**、**§6** 风险红线 |
| `1-dsh-workbuddy-connect/src/upstream.ts` | P0 改动的唯一源码文件（出站头/UA/积分路径/目录双路/两个 parser） |
| `1-dsh-workbuddy-connect/src/client/index.tsx` | 浏览器半入口：`apply()`（卡片 + 探针注册），P1 在此加 section |
| `1-dsh-workbuddy-connect/src/client/WorkBuddyPluginCard.tsx` | 现有卡片（两个变体），P1 后保留为概览入口 |
| `1-dsh-workbuddy-connect/src/client/locales.ts` | `settings.workbuddy` 命名空间的 zh/en 文案 |
| `1-dsh-workbuddy-connect/src/shim.ts` | 同源路由 + loopback 守卫 + 每进程随机密钥 |
| `.probe/eol.mjs` | 查文件行尾（CRLF/LF） |
| `.probe/find-section-slot.mjs` | 按 package.json 找哪个已装包声明了某槽位 |
| `.probe/dump-section-contract.mjs` | 导出槽位声明的原文 |
| `.probe/boot-web*.ps1`、`kill-port.ps1` | 起/停 DSH web（脱离式） |
| `.probe/ui-01-loaded.png`、`ui-02-settings.png`、`ui-03-plugins.png` | 已验证的 UI 截图证据 |

---

## 9. 接手后的第一件事

1. 跑 `cd 1-dsh-workbuddy-connect && pnpm run check`，确认仍是 **341 通过 / 2 失败**（失败项与 §3.4 一致）。若数字不对，先查是不是有人改了 P0 的东西。
2. 按 §3.3 起 DSH web + 浏览器，确认「插件」区仍能看到两张卡片（说明闭环可用）。注意可能已有服务在 3099 跑着（PID 见 §2 残留状态），要重启先 `kill-port.ps1`。
3. 然后按 §6 的 P1 动工。注意 §3.5 的 CRLF 与 client 测试的 tsconfig 归属。

临时/备用 profile 已全部清理：`C:\Users\haishi\.dsh\profiles` 下现在只有 `web`（+ `node_modules`）。**不要为了验证再往别的 profile 里装第二份插件。**
