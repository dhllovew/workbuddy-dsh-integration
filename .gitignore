# ============================================================
# 依赖与构建产物
# ============================================================
node_modules/
.pnpm-store/
coverage/
*.tsbuildinfo

# ============================================================
# 日志与临时文件
# ============================================================
*.log
logs/
*.tmp
*.temp

# ============================================================
# 操作系统 / 编辑器
# ============================================================
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# ============================================================
# 凭据与个人配置 ——【安全红线：以下任何一项都不得入库】
# ============================================================
.env
.env.*
*.key
*.pem
*.p12
*.pfx
credentials*
*secret*
*credential*
settings.yaml
settings.yml

# WorkBuddy / DSH 的真实登录态与本地快照（含 access/refresh token）
.workbuddy-auth.json
.workbuddy-ai-auth.json
.workbuddy-catalog.json
.workbuddy-probe.json
.workbuddy-*.json
workbuddy-desktop.info

# DSH 本地状态目录
.dsh/

# ============================================================
# 第三方上游仓库克隆 —— 不随本仓库分发
# 它们各自有独立的 upstream，请按 README 指引自行 clone。
# 理由：版权归上游作者、仓库体积大（含 node_modules），
#       且本项目并未修改其代码。
# ============================================================
2-workbuddy2api/
3-workbuddy2api-gui/
4-workbuddy-manager/

# ============================================================
# 探查工作区：解包的第三方 tarball 与抓取产物（体积大、可复现）
# 保留手写脚本与 UI 截图证据，仅排除可再生的大体积产物。
# ============================================================
.probe/work/
.probe/*.tgz
.probe/pnpm-list.txt
.probe/test-out.txt
.probe/*.log

# ============================================================
# 本地工具配置
# ============================================================
.ohmyagent/
.agenthub/
.mimosa/
.zcode/
AGENTS.md
